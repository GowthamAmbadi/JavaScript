Conditional Statements:
-->To make the set instructions(block of code)(conditional block) execute only when the given condition is True
-->used to decide whether the code has to be executed or skip based on the given condition
-->line-line(sequenece)

Block of code:set of instructions
-->it will execute only when that specific condition is True

Condition:An expression that evaluates an result(True or False)
Ex:console.log(5>6)

Methods:
if
else 
elseif
switch
Ternary operator

if:only one possible condition

---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <script>
    // let a = 5;
    //     let b = 10;
    //     if(a<b){
    //         document.write("a is smaller than b","<br>")
    //     }
    //     document.write("hellooo","<br>")

    //     if(a>b){
    //         document.write("a is greater than b","<br>")
    //     }
    //     document.write("hellooo")

    // age= prompt("Enter your age")
    //     name=prompt("Enter your name")

    //     if(age<18){
    //         alert(name+ "You are too young to marry")
    //     }else if(age>35){
    //         alert(name+" You are too old to marry")
    //     }else{
    //         alert(name+" You are eligible to marry")
        //}

        let x = 301;
        let y = 198;
        let c = x+y;
        if(x>300 || y>300 && c<500){
            document.write("Team up","<br>");
        }
        else{
            document.write("cannot teamup","<br>");
        }

    
  </script>

  
</body>
</html>
